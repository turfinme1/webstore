{
    "type": "object",
    "name": "reportUI",
    "properties": {
        "title": {
            "type": "string"
        },
        "dataEndpoint": {
            "type": "string"
        },
        "exportConfig": {
            "type": "object",
            "properties": {
                "csv": {
                    "type": "object"
                },
                "excel": {
                    "type": "object"
                }
            }
        },
        "filters": {
            "type": "array"
        },
        "tableTemplate": {
            "type": "string",
            "enum": [
                "groupedHeaders",
                "simple"
            ]
        },
        "headerGroups": {
            "type": "array"
        },
        "columns": {
            "type": "array"
        }
    },
    "required": [
        "title",
        "dataEndpoint",
        "filters",
        "tableTemplate",
        "headerGroups",
        "columns"
    ],
    "additionalProperties": true,
    "errorMessage": {
        "required": {
            "title": "Title is required.",
            "dataEndpoint": "Data endpoint is required.",
            "filters": "Filters are required.",
            "tableTemplate": "Table template is required.",
            "headerGroups": "Header groups are required.",
            "columns": "Columns are required."
        }
    }
}