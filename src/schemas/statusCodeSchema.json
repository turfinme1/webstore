{
    "type": "object",
    "name": "status-codes",
    "routeName": "status-codes",
    "views": "status_codes_view",
    "properties": {
        "code": {
            "type": "integer",
            "description": "The status code value"
        },
        "message": {
            "type": "string",
            "description": "A message associated with the status code"
        }
    },
    "required": [
        "code",
        "message"
    ],
    "additionalProperties": false,
    "errorMessage": {
        "required": {
            "code": "Status code is required.",
            "message": "Message is required."
        },
        "additionalProperties": "Additional properties are not allowed."
    }
}