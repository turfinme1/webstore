{
  "type": "object",
  "name": "appSettingsSchema",
  "properties": {
    "vat_percentage": {
      "type": "number",
      "minimum": 0,
      "maximum": 100,
      "step": 0.01,
      "label": "VAT (%)",
      "placeholder": "Enter the VAT percentage",
      "pattern": "^\\d+(\\.\\d+)?$",
      "errorMessage": {
        "minimum": "VAT percentage must be greater than or equal to 0.",
        "maximum": "VAT percentage must be less than or equal to 100.",
        "type": "VAT percentage must be a number."
      }
    },
    "request_limit": {
      "type": "number",
      "minimum": 1,
      "maximum": 100,
      "default": 10,
      "step": 1,
      "label": "Number of Requests before block (count)",
      "placeholder": "Enter the request limit",
      "pattern": "^\\d+$",
      "errorMessage": {
        "minimum": "Request limit must be greater than or equal to 1.",
        "type": "Request limit must be a number."
      }
    },
    "request_window": {
      "type": "number",
      "default": "10 minutes",
      "minimum": 1,
      "maximum": 100,
      "step": 1,
      "label": "Request Window during which requests are counted for block (minutes)",
      "placeholder": "Enter request window",
      "pattern": "^([0-9]+\\s+(minute|hour|day|week)s?)$",
      "errorMessage": {
        "pattern": "Request window should be a valid interval (e.g., '10 minutes')."
      }
    },
    "request_block_duration": {
      "type": "number",
      "default": "1 hour",
      "minimum": 1,
      "maximum": 100,
      "step": 1,
      "label": "Block Duration (minutes)",
      "placeholder": "Enter block duration",
      "pattern": "^([0-9]+\\s+(minute|hour|day|week)s?)$",
      "errorMessage": {
        "pattern": "Block duration should be a valid interval (e.g., '1 hour')."
      }
    },
    "password_require_digit": {
      "type": "boolean",
      "default": false,
      "label": "Password Require Digits",
      "placeholder": "Require Digits",
      "errorMessage": {
        "type": "This field must be true or false."
      }
    },
    "password_require_lowercase": {
      "type": "boolean",
      "default": false,
      "label": "Password Require Lowercase Letters",
      "placeholder": "Require Lowercase Letters",
      "errorMessage": {
        "type": "This field must be true or false."
      }
    },
    "password_require_uppercase": {
      "type": "boolean",
      "default": false,
      "label": "Require Require Uppercase Letters",
      "placeholder": "Require Uppercase Letters",
      "errorMessage": {
        "type": "This field must be true or false."
      }
    },
    "password_require_special": {
      "type": "boolean",
      "default": false,
      "label": "Require Require Special symbols",
      "placeholder": "Require Special symbols",
      "errorMessage": {
        "type": "This field must be true or false."
      }
    },
    "report_row_limit_display": {
      "type": "number",
      "minimum": 1,
      "maximum": 5000,
      "default": 1000,
      "step": 1,
      "label": "Report Row Limit to be displayed (count)",
      "placeholder": "Enter the report row limit",
      "pattern": "^\\d+$",
      "errorMessage": {
        "minimum": "Report row limit must be greater than or equal to 1.",
        "type": "Report row limit must be a number."
      }
    },
    "campaign_status_update_interval": {
      "type": "number",
      "minimum": 1,
      "maximum": 100,
      "default": 5,
      "step": 1,
      "label": "Campaign Status Update Interval (minutes)",
      "placeholder": "Enter the campaign status update interval",
      "pattern": "^\\d+$",
      "errorMessage": {
        "minimum": "Campaign status update interval must be greater than or equal to 1.",
        "type": "Campaign status update interval must be a number."
      }
    },
    "target_group_status_update_interval": {
      "type": "number",
      "minimum": 1,
      "maximum": 100,
      "default": 5,
      "step": 1,
      "label": "Target Group Status Update Interval (minutes)",
      "placeholder": "Enter the target group status update interval",
      "pattern": "^\\d+$",
      "errorMessage": {
        "minimum": "Target group status update interval must be greater than or equal to 1.",
        "type": "Target group status update interval must be a number."
      }
    },
    "target_group_status_update_initial_time": {
      "type": "string",
      "format": "time",
      "default": "00:00",
      "label": "Target Group Status Update Initial Time",
      "placeholder": "Enter the target group status update initial time"
    },
    "user_group_chart_count": {
      "type": "number",
      "minimum": 1,
      "maximum": 100,
      "default": 10,
      "step": 1,
      "label": "User Group Chart Count",
      "placeholder": "Enter the user group chart count",
      "pattern": "^\\d+$",
      "errorMessage": {
        "minimum": "User group chart count must be greater than or equal to 1.",
        "type": "User group chart count must be a number."
      }
    },
    "campaign_chart_count": {
      "type": "number",
      "minimum": 1,
      "maximum": 100,
      "default": 10,
      "step": 1,
      "label": "Campaign Chart Count",
      "placeholder": "Enter the campaign chart count",
      "pattern": "^\\d+$",
      "errorMessage": {
        "minimum": "Campaign chart count must be greater than or equal to 1.",
        "type": "Campaign chart count must be a number."
      }
    },
    "push_notification_provider_id": {
      "type": "string",
      "label": "Push Notification Provider",
      "placeholder": "Enter the push notification provider",
      "pattern": "^[0-9]+$",
      "errorMessage": {
        "pattern": "Push notification provider ID must be numeric."
      }
    }
  },
  "required": [ "request_limit", "request_window", "request_block_duration", "vat_percentage", "report_row_limit_display" ],
  "additionalProperties": false,
  "errorMessage": {
    "required": {
      "request_limit": "Request limit is required.",
      "request_window": "Request window is required.",
      "request_block_duration": "Block duration is required.",
      "vat_percentage": "VAT percentage is required.",
      "report_row_limit_display": "Report row limit is required."
    },
    "additionalProperties": "No additional properties are allowed."
  }
}
